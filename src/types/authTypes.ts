export interface RegisterGooglePayload{
    userName: string;
    email: string;
    password: string;
    rePassword: string;
    providerId: string;
    provider: string;
}

export interface RegisterPayload{
    userName: string;
    email: string;
    password: string;
    rePassword: string;
}